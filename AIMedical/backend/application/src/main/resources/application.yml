spring:
  application:
    name: aimedical-sys
  profiles:
    active: phase1,dev
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/aimedical?useUnicode=true&characterEncoding=utf8&useSSL=true&serverTimezone=Asia/Shanghai}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: false
    show-sql: false

  sql:
    init:
      mode: never
      schema-locations: classpath:db/schema.sql
      data-locations: classpath:db/data.sql

server:
  port: 8080
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true

# JWT配置
jwt:
  secret: ${JWT_SECRET:aimedical-sys-jwt-secret-key-for-development-only-min-256bits}
  access-token-expiration: 900
  refresh-token-expiration: 604800

# AI 超时配置
ai:
  platform:
    enabled: true
  timeout:
    triage: 8
    prescription-audit: 6
    medical-record-generate: 12
    prescription-assist: 8
  mock:
    response-strategy: STATIC

# 外部门面超时
consultation:
  doctor-facade:
    timeout: 2

medical-record:
  visit-facade:
    timeout: 2
