# 再审议判定报告（v3）

## 判定结果

RETRY

## 判定理由

质询报告不存在，组件B在第1轮（最大12轮）提前终止。诊断报告包含2项严重问题（1.1 内存存储未隔离导致Phase 5迁移成本未规避、2.1 check-dose异步AI去重/节流策略缺失）和5项一般问题，根据判定标准"审查报告包含严重或一般等级的问题"应重新运行组件A。

## 需要解决的问题（仅 RETRY 时存在）

- **问题描述**："规避 Phase 5 迁移成本"目标在内存存储维度未闭合，三项内存存储直接使用 ConcurrentHashMap 实现，无隔离层
- **所在位置**：§1.1 line 9；§6.1 line 991-995
- **严重程度**：严重
- **改进建议**：将 Store 抽象层从"建议"升级为"设计强制项"，Phase 2/3 使用 ConcurrentHashMapStore 实现，Phase 5 替换为 RedisStore 实现

- **问题描述**：check-dose 端点高频调用下的异步 AI 去重/节流策略完全缺失，一次编辑会话可能产生数十至数百个并发 AI 调用
- **所在位置**：§3.4 line 598-601；§4.4 line 830-835；§6.3 line 1001-1003
- **严重程度**：严重
- **改进建议**：定义 prescriptionId 级别的异步 AI 调用去重策略，或定义前端防抖/后端限流配合的节流规则，或定义覆盖式更新及取消前次 task 的机制

- **问题描述**："规则可配置"依赖 admin 模块 OOD，无时间线协调，存在不可控交付风险
- **所在位置**：§9.3 line 1158-1168
- **严重程度**：一般
- **改进建议**：在 §1.1a 外部依赖表中补充 admin 模块规则管理接口的依赖项，标注实现时间线约束和风险说明

- **问题描述**：AiResult.success=true 且 data=null 的边界处理未定义，Service 层解引用可能引发 NullPointerException
- **所在位置**：§2.3 line 344-345；§3.1 line 358；§3.2 line 452-454；§3.3 line 568-572
- **严重程度**：一般
- **改进建议**：在各 Service 实现类中增加 data=null 的防御性检查，或在 AiResult 类中增加工厂方法约束

- **问题描述**："撤销审核"的操作触发端点未定义，无法直接指导编码
- **所在位置**：§3.2 line 482-483；§4.2 line 759-761
- **严重程度**：一般
- **改进建议**：新增撤销审核的 API 端点定义，或将撤销行为纳入已有端点

- **问题描述**：prescriptionId 在 check-dose 请求链路中的传递路径未闭环，首次 check-dose 调用时 prescriptionId 可能尚不存在
- **所在位置**：§1.3 line 127-128；§3.4 line 627-629；§4.4 line 826-841
- **严重程度**：一般
- **改进建议**：明确定义 prescriptionId 在处方编辑生命周期中的分配时机及创建策略

- **问题描述**：缺少 AI Mock 实现的行为契约，影响本地开发效率
- **所在位置**：§1.2 line 41；§2.1 line 239-247
- **严重程度**：一般
- **改进建议**：在 §2.3 AiService 接口定义后新增"Mock 实现契约"段落，定义 MockAiService 的返回策略和激活方式
