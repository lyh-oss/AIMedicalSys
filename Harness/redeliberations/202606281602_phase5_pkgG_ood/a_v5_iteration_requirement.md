根据以下审查结果，迭代上一轮的产出，形成新版的文件，从而更好地满足用户需求。

## 当前审查结果

### 问题 1：【严重】修订说明与设计正文混合，产出不是一份可交付的 OOD 设计文档
- **描述**：`a_v4_copy_from_v3.md` 中，修订说明从 `§修订说明（v2）` 至 `§修订说明（v4 — 本轮修订）`，累计约 1500+ 行，占文档近半篇幅。实现者无法区分"当前设计"与"已废弃的旧提案"，文档定位混乱。
- **严重程度**：严重
- **改进建议**：
  1. 将全部修订说明剥离为独立归档文件（如 `design_evolution_log.md`），从产出正文中彻底删除
  2. 产出正文只保留最终设计状态
  3. 若需保留变更可追溯性，在文档开头用不超过 5 行的"变更摘要"表替代

### 问题 2：【重要】文档内部散布迭代标记，残留大量过程性注记
- **描述**：正文散布着大量版本标记如 `(v4 新增)`、`(v5 新增)`、`(v8 修正)`、`(v11 修正)`、`(v14 修正)`、`(v20 新增)`、`(v23 补充)`、`(v24 补充)` 等，遍布 §3.1–§3.11 全文。实现者阅读时无法判断哪个是最终设计。
- **严重程度**：重要
- **改进建议**：
  1. 全文搜索并清除所有 `(v{N} 新增/修正/补充/修订)` 类标记，替换为空白
  2. 标题版本号改为最终稳定版本号（如移除 "v4" 后缀或固定为 "v1.0"）
  3. 在文档元信息区（如文档头部）保留一行来源说明即可

### 问题 3：【重要】§3.11 节编号不连续且章节位置异常
- **描述**：§3.11 节内编号从 `3.11.3` 直接跳到 `3.11.5`（缺少 `3.11.4`）；§3.11 实际位于 §11 之后（行 3335），而非按顺序在 §3.10 之后。
- **严重程度**：重要
- **改进建议**：
  1. 将 §3.11 整体移至 §3.10 之后
  2. 重新编号 §3.11.x 为连续序列（1–7，无跳跃）
  3. 修复所有交叉引用

### 问题 4：【重要】缺少实施者端的"首次编码引导"信息
- **描述**：§1.6 API Surface 状态表虽按 P0–P3 标注优先级，但未提供最小可工作原型路径、组件编译依赖拓扑排序、Mock/Stub 策略建议。
- **严重程度**：重要
- **改进建议**：
  1. 在 §1.6 之后新增"实施拓扑顺序"小节，给出拓扑排序后的建议实施批次及每批次的验收标准
  2. 对 P0 组件进一步细分首批（如先实现 `SlidingWindowMetricsStore` → 再实现 `DegradationStrategy` 骨架 → 再实现 `AbstractCapabilityExecutor`）
  3. 补充每个批次的最小验证手段（如单元测试通过标准）

### 问题 5：【中等】需求响应中"风格一致性"参照缺乏具体对照
- **描述**：§1.2 末声明"已参照 Phase0、Phase1ABD"，但未给出具体的风格一致性检验清单和跨阶段对照表，实现者无法验证一致性。
- **严重程度**：中等
- **改进建议**：
  1. 在 §1.2 或新增 §1.7 中列出 3–5 条具体的风格一致性规则
  2. 为每条规则标注来自哪个前序设计的哪一节

### 问题 6：【中等】doExecuteInternal() 伪代码中 structuredChat/chat 返回值的两层解包缺乏异常传播说明
- **描述**：§4.1 中 `structuredChat()` 和 `chat()` 调用通过 `.join().getData()` 两层解包，缺少对 `CompletionException` 的捕获说明，缺少对 `AiResult` 状态的检查。
- **严重程度**：中等
- **改进建议**：
  1. `.join()` 调用外层包裹 try-catch，区分 `CompletionException` 和非 `CompletionException`
  2. `CompletionException` 中拆解出原始异常，按分类重新路由到对应 catch 分支
  3. 补充 `AiResult` 状态检查：`.getData()` 前验证 `AiResult.isSuccess()`

### 问题 7：【中等】未定义 AiService 接口自身的线程安全契约
- **描述**：`AiService` 接口被 13 个业务模块同时并发调用（Spring 默认单例），但接口层面上未定义调用方是否需要保证每次调用使用独立的 request 实例、返回值 `CompletableFuture` 完成后 `AiResult` 的可变性契约。
- **严重程度**：中等
- **改进建议**：
  1. 在 §1.3 `AiService` 职责描述中补充线程安全契约（建议：实现需线程安全；request 应为只读，调用方无需额外同步）
  2. 在 `FallbackAiService` 描述中补充装饰器层面的线程安全保证

### 问题 8：【轻微】§3.11 共同约束中关于 DTO 改造的约定与 §3.5 过渡策略存在细微不一致
- **描述**：§3.11 共同约束声明了 DTO 基类继承改造要求，但未引用 §3.5 定义的 Jackson 兼容性测试要求，实现者可能完成 DTO 改造但遗漏配套测试。
- **严重程度**：轻微
- **改进建议**：
  1. §3.11 共同约束中增加一行指向 §3.5 的 Jackson 兼容性测试要求
  2. 或直接在 §3.11 末尾补充"各能力 DTO 改造完成后需执行 §3.5 定义的 3 个兼容性验证场景"

## 历史迭代回顾

### 已解决的问题（出现在历史反馈但当前反馈中不再提及）
- **第2轮**：ai-api "不变"声明与变更的系统性矛盾 → 已通过新增"ai-api 变更范围总结表"解决
- **第2轮**：多实例行为未定义 → 已通过新增§1.5"多实例行为约束"子章节解决
- **第2轮**：API Surface 状态表缺失 → 已通过新增§1.6"API Surface 状态表"解决
- **第2轮**：Jackson 兼容性测试断言不可验证 → 已替换为3个真实验证场景
- **第2轮**：structuredChat tool_use 传递方式未定义 → 已补充 tools 字段和检测策略
- **第2轮**：FallbackAiService 条件开关缺少 YAML 绑定 → 已推荐 @Value 构造器注入
- **第3轮**：@Qualifier 注入不可行的事实错误 → 已修正为 Map 方案
- **第3轮**：降级策略注入两套并行描述 → 已统一为一套机制
- **第3轮**：底座能力条件化注册不一致 → 已明确 @Component 自注册策略
- **第3轮**：薄适配器 doExtractDepartmentId 回退描述错误 → 已修正
- **第3轮**：ExperimentGroup 未正式定义 → 已补充字段定义
- **第3轮**：AiCallLogStats 未正式定义 → 已补充字段定义及索引策略
- **第3轮**：PrescriptionLocalRuleFallback 未定义 → 已补充行为契约
- **第3轮**：ModelRoute.parameters 扩展点缺失 → 已补充参数映射规则
- **第3轮**：DiscussionConclusion 前置 LLM 超时未定义 → 已补充契约
- **第3轮**：extractCallerRole 过滤规则未定义 → 已补充显式规则

### 持续存在的问题（在多轮反馈中反复出现，需重点解决）
- **问题1（修订说明混合正文）**：第4轮已发现，本轮再次检出。反复提及说明该问题未被有效修复。
- **问题2（迭代标记散布）**：第4轮已发现，本轮再次检出。需全文清除所有(v{N})类标记。
- **问题3（§3.11 编号/位置）**：第3轮（覆盖范围不对称）、第4轮（编号不连续）、本轮（编号不连续且位置错误）反复提及。结构性问题在多轮中持续存在。
- **问题4（编码引导缺失）**：第4轮已发现，本轮再次检出。需在§1.6后新增实施拓扑顺序。
- **问题5（风格一致性参照）**：第4轮已发现，本轮再次检出。需补充具体规则清单。
- **问题6（异常传播说明）**：第4轮已发现，本轮再次检出。需补充异常捕获与 AiResult 状态检查。
- **问题7（线程安全契约）**：第4轮已发现，本轮再次检出。需补充接口层面契约。

### 新发现的问题（本轮新识别）
- **问题8（DTO改造约定不一致）**：本轮首次检出。§3.11 未引用 §3.5 的 Jackson 兼容性测试要求。

## 上一轮产出路径
C:\Develop\Software\AIMedicalSys\Harness\redeliberations\202606281602_phase5_pkgG_ood\a_v4_copy_from_v3.md

## 用户需求
C:\Develop\Software\AIMedicalSys\Harness\redeliberations\202606281602_phase5_pkgG_ood\requirement.md
