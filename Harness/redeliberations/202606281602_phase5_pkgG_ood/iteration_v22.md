# 再审议判定报告（v22）

## 判定结果

RETRY

## 判定理由

诊断报告包含 4 个质量问题，其中 1 个严重（Problem 1：`doDegrade` 方法定义与所有调用点签名不一致，半完成迁移态导致不可编译）、2 个重要（Problem 2：§4.2 catch 块缺少过渡期双阶段异常检测，与 §3.1 契约不一致；Problem 3：v22 修订说明 5 项修改未在正文落地）、1 个一般（Problem 4：预检降级路径调用点参数错位风险）。质询报告结果 LOCATED，确认了上述问题的有效性。内部循环实际轮次 2 < 最大轮次 12，说明提前终止于 LOCATED 状态。据判定标准，审查报告包含严重和一般等级的问题，判定为 RETRY。

## 需要解决的问题（仅 RETRY 时存在）

- **问题描述**：`doDegrade` 方法定义（7 参数 CallContext 签名，§4.1 第 3357 行）与所有调用点（约 17 处，15 参数旧签名）不一致，类图（§2.3 第 461 行）使用 CallContext 签名，三者处于半完成迁移态，不可编译
- **所在位置**：§2.3 类图第 461 行；§4.1 第 3357 行（定义）；§4.1 约 15 处调用点；§4.2 约 2 处调用点
- **严重程度**：严重
- **改进建议**：统一三端签名：方案 A（推荐）回退类图和方法定义为旧 15 参数签名，或方案 B 将所有调用点更新为 7 参数 CallContext 签名

- **问题描述**：§4.2 薄适配器 catch 块仅含单阶段 `instanceof` 检测，缺少 §3.1 契约定义的两阶段异常检测（第 2 阶段 `isKnownPhase4BusinessException()` 包路径前缀回退），过渡期内业务异常将被误分类为基础设施异常
- **所在位置**：§4.2 第 3572–3584 行 vs §3.1 第 1101–1127 行（契约定义）和第 1278–1294 行（模板方法伪代码）
- **严重程度**：重要
- **改进建议**：将 §4.2 catch 块替换为与 §3.1 模板方法一致的两阶段判定

- **问题描述**：v22 修订说明（第 4462–4471 行）列出 5 项变更（CallContext 旧签名保持、parseTimeout 路径修正、版本标记清理、AiOrchestrator 临界条件修正、estimateTokens 精确 Tokenizer 分支），但正文内容未做对应修改
- **所在位置**：尾部第 4462–4471 行 vs 正文对应位置
- **严重程度**：重要
- **改进建议**：将 5 项变更实际应用到正文，或在头部显式标注修订说明描述的是计划中变更、正文尚未实施

- **问题描述**：§3.1 第 1344 行 `doDegrade()` 预检降级路径调用点末尾传 `null, null`，在旧 15 参数签名下参数位置存在隐晦的错位风险
- **所在位置**：§3.1 第 1344 行（预检降级路径调用点）与 §4.1 第 3357 行（方法定义）对比
- **严重程度**：一般
- **改进建议**：在三端签名统一后，确保所有调用点参数数量、位置与定义完全一致
