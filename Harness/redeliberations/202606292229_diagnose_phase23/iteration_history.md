## 迭代第 1 轮

1. **问题描述**：P05将OOD中不存在的要求归因于OOD（riskLevel/alerts/auditRecordId/prescriptionHash字段非SubmitResponse定义）
   - 所在位置：a_v1_diag_v2.md:173
   - 严重程度：严重
   - 改进建议：修正P05中关于OOD要求的描述，准确引用OOD §3.2 SubmitResponse的实际字段定义（submitted/prescriptionOrderId/blockInfo/errorCode）
2. **问题描述**：M04根因分类错误——将实现编码问题归因于OOD设计问题
   - 所在位置：a_v1_diag_v2.md:267-270
   - 严重程度：严重
   - 改进建议：将根因分类修改为"实现编码问题"，修改建议改为"将OptimisticLockException捕获移至UPDATE/merge路径，INSERT路径移除此类异常处理"
3. **问题描述**：C23修复建议与代码实际依赖矛盾——setChiefComplaint等操作为AI请求构建所必需，移至AI调用后将导致请求缺失必要数据
   - 所在位置：a_v1_diag_v2.md:141
   - 严重程度：一般
   - 改进建议：细化修复建议，区分两类session操作：(a)AI请求构建必需的前置操作保留在AI调用前；(b)仅非AI必需的累积状态更新在TriageRecord持久化后执行
4. **问题描述**：缺少系统性的优先级排序，61个问题未按影响面/修复成本分组
   - 所在位置：a_v1_diag_v2.md全文
   - 严重程度：一般
   - 改进建议：在报告开头增加优先级分组表（P0必须立即修复/P1严重影响业务逻辑/P2可并行修复），按严重程度和依赖关系分群

## 迭代第 2 轮

1. **问题描述**：C04 修复建议中建议为 triage()/selectDepartment()/saveTriageRecord() 添加 @Transactional，存在长事务风险（AI 调用最长 8 秒）、private 方法 @Transactional 无效、与 OOD 原始意图不符三方面问题
   - 所在位置：C04 节
   - 严重程度：严重
   - 改进建议：将 saveTriageRecord() 中的持久化逻辑抽取为单独的事务方法（如 `@Transactional persistRecord()`），或使用 TransactionTemplate 编程式事务仅包围 TriageRecordRepository.save() 调用；在报告中增加对事务边界与 AI 调用时序的约束说明
2. **问题描述**：C14 修复建议仅说"未判断 retryCount >= maxRetryCount 时将状态迁移到 EXPIRED"，但未说明判断时机，可能导致递增后→迁移前的窗口内进程崩溃时补偿超限
   - 所在位置：C14 节
   - 严重程度：一般
   - 改进建议：明确执行顺序为：(1) 补偿尝试前先判断 retryCount >= maxRetryCount；(2) 若已达上限直接迁移 EXPIRED 不尝试补偿；(3) 若未达上限执行补偿；(4) 递增 retryCount
3. **问题描述**：C23 "将 AI 输入准备阶段的 session 修改与持久化路径解耦"的表述仍偏抽象，执行者缺少精确时序描述
   - 所在位置：C23 节
   - 严重程度：一般
   - 改进建议：补充精确时序表：[1] 行 72-80 session 修改（保持原位）→ [2] 行 82-93 AI 调用 + 超时/降级 → [3] 行 95-108 处理 AI 结果 → [4] saveTriageRecord（事务内）→ [5] 行 140 session.setAiFailCount(0) → [6] session.setCorrectedChiefComplaint(aiData.getCorrectedChiefComplaint())

## 迭代第 3 轮

1. **问题描述**：P05 错误码处理方向给出二选一模糊建议，且 [代码修改] 与 [OOD 文档修改] 存在自洽矛盾
   - 所在位置：`a_v3_diag_v1.md:188`（P05 `[代码修改]` 段末句）
   - 严重程度：一般
   - 改进建议：将模糊建议改为明确单向建议 — WARN 路径不使用 errorCode，改为通过 `SubmitResponse.warnResult` 承载风险信息，移除 `RX_AUDIT_WARN_REQUIRES_FORCE_SUBMIT` 错误码或明确其兜底降级场景
2. **问题描述**：A07 与 A09 的跨问题耦合未分析，可能产生修复冲突
   - 所在位置：`a_v3_diag_v1.md:392-408`（跨问题耦合副作用分析章节）
   - 严重程度：一般
   - 改进建议：增加 A07×A09 耦合条目，约定修复顺序：(1) 先修复 A09 调用方前置检查 + 降级路径；(2) 再修复 A07 的 `Objects.requireNonNull(data)` 断言；(3) 说明 `AiResult.failure()` 的 `partialData` 可能为 null，不受断言约束


## 迭代第 4 轮

1. **问题描述**：C23中"恢复前序修改"表述与实际操作时序存在逻辑矛盾
   - 所在位置：P1 > C23 > 实际根因 第二句
   - 严重程度：中等
   - 改进建议：将"恢复前序修改"改为"增量追加AI结果"的表述，避免执行者误以为需要回退操作

2. **问题描述**：跨问题耦合分析遗漏三组隐含依赖关系
   - 所在位置：跨问题耦合副作用分析 章节
   - 严重程度：一般
   - 改进建议：补充C06/E03×C17、P02/E06×P06×P04/E04、A02×A10三组耦合分析，并增加修复依赖总览表

3. **问题描述**：C01、M01、C08的P1优先级与影响面不匹配
   - 所在位置：P1 分组
   - 严重程度：一般
   - 改进建议：将C01（缺少correctedChiefComplaint快照字段）、M01（4个错误码缺失）、C08（接口签名错误）上调至P0

## 迭代第 5 轮

1. **问题描述**：C23 优先级（P0）与问题实际性质（说明性条目，非独立代码缺陷）不匹配，可能导致执行者错误分配紧急修复资源
   - 所在位置：a_v5_diag_v1.md:107-118（C23 条目）
   - 严重程度：中等
   - 改进建议：将 C23 降级至 P2 或移入耦合分析附注；标题标注"说明性条目"避免误解；若保留 P0 需指明具体代码变更

2. **问题描述**：correctedChiefComplaint 数据流链路（C01×C03×C19×A04×C23）未被纳入跨问题耦合分析群组，缺失修复顺序约束可能导致中间状态不一致
   - 所在位置：a_v5_diag_v1.md:401-445（耦合分析章节）
   - 严重程度：一般
   - 改进建议：新增 correctedChiefComplaint 数据流耦合群组，约定 C01→A04→C03→C19→C23 修复顺序

3. **问题描述**：P05 的`[可编码并行]`标注与 OOD 文档修改尚未确认的实际依赖矛盾，存在开发者自行推测字段类型导致返工的风险
    - 所在位置：a_v5_diag_v1.md:185-198（P05 条目）
    - 严重程度：一般
    - 改进建议：将 P05 代码子项标注改为`[OOD 文档确认后实施]`，或为文档修改与代码修改分配同一任务标识强制串行

## 迭代第 6 轮

1. **问题描述**：P01（异步AI调度机制未实现）和A03（AiSuggestionResult状态映射表全部未实现）均为P0条目，但只描述问题现象和引用证据，未给出任何修复方向或实施策略
   - 所在位置：a_v6_diag_v1.md:58-63（P01）、a_v6_diag_v1.md:68-73（A03）
   - 严重程度：中等
   - 改进建议：为P01补充推荐实施路径；为A03补充状态机实现策略

2. **问题描述**：A02（P0）与A10（P2）的耦合分析约定A10必须"先行或同期"于A02，但A10因P2优先级可能被长期搁置，导致配置键永不写入application.yml，报告未警示此风险也未建议优先级调整
   - 所在位置：a_v6_diag_v1.md:64-67（A02）、a_v6_diag_v1.md:370-382（A10）、耦合分析A02×A10节（行444-448）
   - 严重程度：一般
   - 改进建议：将A10中涉及ai.timeout.*和facade.*.timeout的配置键定义为A02的前置子任务，标注"与A02同迭代完成"或赋予独立P1优先级

3. **问题描述**：P01（异步AI调度未实现）和A03（状态映射表未实现）共同指向DedupTaskScheduler的异步AI调度管线，是同一段缺失业务逻辑的两个视图，耦合分析章节缺少该群组
   - 所在位置：a_v6_diag_v1.md:395-448（跨问题耦合副作用分析），缺少P01×A03条目
   - 严重程度：一般
   - 改进建议：新增P01×A03耦合条目，约定同步实施不可拆分

4. **问题描述**：A07（AiResult.success(data)允许data=null）和A11（业务层防御性判空）根因修复完全相同，但耦合分析未关联A11，可能导致重复派发修复任务或A11先于A09移除判空导致NPE风险
   - 所在位置：a_v6_diag_v1.md:360-364（A07）、a_v6_diag_v1.md:383-386（A11）、耦合分析A07×A09节（行399-405）
   - 严重程度：一般
   - 改进建议：将A07和A11合并在同一条目下，在耦合分析中增加A07×A09×A11三向约束

5. **问题描述**：推荐在findBySessionId查询上加@Lock(PESSIMISTIC_WRITE)但未分析悲观锁对分诊流程吞吐量的影响，也未讨论替代方案
   - 所在位置：a_v6_diag_v1.md:420-424（C04×E02×S04耦合分析修复实施顺序第3步）
   - 严重程度：轻微
   - 改进建议：添加性能影响评估段或锁策略的进一步考量
